To create file and insert text directions:

* touch = to create the file name
* vi (filename) = to get into file
* i = insert text into file

The git command used to update the file up_to_date from github onto local machine:

* cd = change directory to alx-zero_day
* cd = change directory to 0x03-git
* git branch -v = to which branch im currently in
* git pull origin main = to get the updates from github page

